apiVersion: v1
kind: Service
metadata:
  name: cicd-pipeline
  labels:
    app: cicd-pipeline
spec:
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: 3000
  selector:
    app: cicd-pipeline
